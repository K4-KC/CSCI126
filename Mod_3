clc
clear

disp('Find range of projectile (max_x) and the maximum height (max_y)')
v = input('Enter initial velocity in m/s: ');
angle = input('Enter angle in degrees: ');

g = 9.8;

max_x = v^2 * sind(2 * angle) / g
max_y = v^2 * sind(2 * angle)^2 / (2 * g)

disp('Kanva')
